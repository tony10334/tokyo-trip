/* ============================================================
   設定檔
   ------------------------------------------------------------
   這個檔會跟著網站一起公開,所以裡面【不放任何密碼、token、
   私人資料】。共享房號是在網站上按鈕產生的,只存在你們自己的
   手機和分享網址裡,不會進到這份原始碼。
   ============================================================ */

const CONFIG = {

  /* ---------- 旅程基本資料 ---------- */
  tripTitle: '東京 7日6夜',
  startDate: '',      // 例：'2026-11-15'。填了日卡會自動顯示日期與星期
  members: ['我', '同伴A', '同伴B', '同伴C'],   // 網站上也能改
  defaultRate: 4.7,   // 1 TWD = ? JPY,網站上也能改


  /* ---------- 4 人共享 ----------
     'shared' = 開啟共享（預設）。第一次用的人到「資訊」分頁按
                「建立共享房間」,拿到網址丟給另外 3 個人就好。
                不用註冊、不用金鑰。
     'local'  = 關掉共享,資料只留在自己這台裝置。            */
  mode: 'shared',
};
